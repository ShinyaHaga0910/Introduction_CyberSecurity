# 図の編集元

本文図は`build/generate_figures.py`で生成する。色だけに依存せず、すべてのboxと矢印に文字labelを付ける。出力は`assets/figures/fig01...fig19.png`。

各図の意味、参照章、事実確認先は`instructor/figure-register.md`へ記録する。生成後はA4 PDFへ組み込み、印刷寸法で文字を確認する。
