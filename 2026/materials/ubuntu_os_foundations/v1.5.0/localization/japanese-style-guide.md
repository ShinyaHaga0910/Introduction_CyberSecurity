# 日本語スタイルガイド（Ubuntu・OS基礎教材）

本書は、Linux未経験の大学生が基礎から無理なく理解できるよう、また将来的に英語原本からウズベク語・ロシア語への多言語展開をスムーズに行えるよう策定された日本語用字用語・文体ガイドラインである。

---

## 1. 基本方針と文体

1. **「である」調（常体）の統一**
   - 本文はすべて「である」「だ」調（常体）で統一する。「です・ます」調（敬体）や不自然な体言止め（名詞止め）は原則として排除する。
   - 断定を避けるための過度な「～と思われる」「～かもしれない」の多用を避け、客観的な事実と理由を明瞭に述べる。

2. **対象読者への配慮**
   - 読者はIT/Linux未経験の大学生である。
   - 専門用語をいたずらに羅列せず、「なぜその仕組みが必要なのか」「裏で何が起きているのか」の因果関係が直感的に伝わる構文とする。

3. **翻訳容易性（Translatability）の確保**
   - 将来の英語・ウズベク語・ロシア語への翻訳を見据え、以下の点に配慮する：
     - **主語と述語の対応を明確にする**（省略された主語によって動作主体が曖昧になることを防ぐ）。
     - **文化依存の比喩や日本独自のスラングを避ける**。
     - **同一概念に対する表記揺れを徹底的に排除する**。
     - **一文を簡潔にし、修飾関係が複雑な重文・複文を避ける**。

---

## 2. 用語の採用基準と対訳ルール

日本の大学IT教育および現場で標準的とされるカタカナ表記を採用し、英単語の不自然な混在を解消する。初出時には必要に応じて日本語と英語を併記（例: `カーネル（kernel）`）し、以後は統一した日本語表記を用いる。

### 2.1 主要技術用語の統一表記

| 元の英単語 | 採用表記 | 補足・使い分け |
| :--- | :--- | :--- |
| file | **ファイル** | 文脈により設定ファイル、実行ファイルなど |
| directory | **ディレクトリ** | フォルダではなく「ディレクトリ」に統一 |
| user | **ユーザー** | 末尾に長音記号「ー」を入れる（JIS規格・現代IT標準） |
| group | **グループ** | primary group: 主グループ、supplementary group: 補助グループ |
| command | **コマンド** | コマンドライン、外部コマンド、組み込みコマンド |
| process | **プロセス** | プログラムの実行単位。PID（プロセスID） |
| service | **サービス** | バックグラウンドで動作する常駐管理単位 |
| socket | **ソケット** | プロセスとカーネル通信の出入口 |
| port | **ポート** | ポート番号、通信の宛先 |
| log | **ログ** | 動作記録。ログファイル、ジャーナルログ |
| package | **パッケージ** | ソフトウェアの配布・導入単位 |
| kernel | **カーネル** | OSの中核プログラム |
| shell | **シェル** | コマンド解釈実行プログラム（Bash等） |
| path | **パス** | 絶対パス（absolute path）、相対パス（relative path） |
| resource | **リソース** | CPU、メモリなどの計算資源 |
| interface | **インターフェース** | 操作入口、境界面 |
| permission | **アクセス権** / **パーミッション** | **原則として「アクセス権」を使用**。モード値（rwx）やビット設定などファイルシステムの属性そのものを指す技術的文脈では「パーミッション」を併用・許容 |
| hardware | **ハードウェア** | 物理装置全般 |
| software | **ソフトウェア** | プログラム全般 |
| memory | **メモリ** | 主記憶装置。長音なし「メモリ」 |
| storage | **保存領域** / **ストレージ** | 初心者向けには「保存領域（ストレージ）」 |
| device | **デバイス** | 周辺装置 |
| network | **ネットワーク** | 通信路・通信網 |
| system | **システム** | システムコール、システム管理 |
| option | **オプション** | コマンドの動作変更フラグ（引数と区別） |
| argument | **引数（ひきすう）** | コマンドに渡す対象値（オプションと区別） |
| status | **ステータス** | 終了ステータス（終了コード、exit status） |
| client | **クライアント** | 接続要求側 |
| server | **サーバー** | 待受・サービス提供側 |
| host | **ホスト** | コンピューター端末・接続先 |
| local | **ローカル** | 手元の実行環境（CloudShellなど） |
| remote | **リモート** | 接続先の遠隔環境（Ubuntuなど） |
| account | **アカウント** | ユーザー等の登録情報 |
| session | **セッション** | ログインからログアウトまでの一連の対話環境 |
| prompt | **プロンプト** | コマンド入力待ちの表示文字列 |
| script | **スクリプト** | 一連の処理を記述した実行ファイル |
| stream | **ストリーム** | 標準入力、標準出力、標準エラー出力 |
| pipeline / pipe | **パイプ** | コマンド同士の標準入出力をつなぐ仕組み（記号 `\|`） |
| redirect | **リダイレクト** | 入出力の向き先をファイル等へ切り替える仕組み |
| signal | **シグナル** | プロセスへの通知・制御割り込み |
| repository | **リポジトリ** | パッケージの配布元保管庫 |
| unit | **ユニット** / **unit** | systemdの設定・管理単位（文脈によりunitファイル等） |
| low layer | **低レイヤー** | 「低い層」ではなく「低レイヤー」とする |
| user space | **ユーザー空間** | カーネルから分離された通常プログラムの実行領域 |
| system call | **システムコール** | ユーザー空間からカーネル機能を利用する要求・入口 |
| distribution | **ディストリビューション** | Linuxカーネルとツール群を統合したOSパッケージ体系 |
| version | **バージョン** | 版・版番号 |
| authentication | **認証** | 本人確認（誰であるか） |
| authorization | **認可** | 権限付与（何を許可するか） |
| working directory | **カレントディレクトリ** / **作業ディレクトリ** | 現在位置のディレクトリ |
| home directory | **ホームディレクトリ** | 各ユーザー専用の拠点ディレクトリ |

---

## 3. 英語表記を維持する語（固有名詞・プロトコル・規格）

以下の語句は、IT分野の共通識別子・国際標準・固有名詞であるため、カタカナ化せず英大文字または原表記のまま維持する。

- **OS名・プロジェクト名**:
  `Linux`, `Ubuntu`, `Unix`, `Debian`, `Fedora`, `Windows`, `macOS`, `Darwin`, `BSD`, `GNU`
- **ソフトウェア・ツール名**:
  `systemd`, `systemctl`, `journald`, `journalctl`, `APT`, `Bash`, `nano`, `OpenSSH`
- **プロトコル・ネットワーク規格**:
  `SSH`, `SCP`, `HTTP`, `TCP`, `IP`, `IPv4`, `IPv6`, `UDP`, `DNS`
- **操作形態・識別子**:
  `GUI`, `CLI`, `PID`, `PPID`, `UID`, `GID`
- **AWS関連サービス・機能名**:
  `AWS CloudShell`, `Session Manager`, `EC2`, `IAM`, `ProxyCommand`
- **コマンド名・コード表記**:
  すべてのLinuxコマンド名（`ls`, `grep`, `cat`, `curl`, `ss`, `kill`, `chmod`, `chown`, `stat`, `namei` など）およびインラインコード内のオプション、パス名、環境変数名、Unit設定キー（`User=`, `ExecStart=` など）

---

## 4. 変更禁止対象と保全ルール

校閲および文体修正において、以下の要素を厳格に保全し、改変してはならない：

1. **インラインコードおよびコードブロック**
   - コマンド文字列、オプション引数、実行例、設定ファイルのコードブロックは一字一句変更しない。
2. **ファイルパス・識別子**
   - `/srv/jdu-share` などのパス名、`jdu-web.service` などのユニット名、`ssm-user` などのユーザー名・グループ名、チェックID（`M0`〜`M7`, `P1`〜`P6` など）は完全に維持する。
3. **リンクおよび画像参照パス**
   - 相対リンク（`../../../assets/figures/...` や `[参考資料](https://...)`）は変更しない。
4. **章節の構成・順序・技術的事実**
   - 見出しのレベル（`#`, `##`, `###`）や説明の順序、節立ては変更しない。
   - 要約や勝手な削除を行わず、説明の明確化に必要な最小限の語句補正にとどめる。
