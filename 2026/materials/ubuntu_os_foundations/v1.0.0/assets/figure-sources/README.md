# 図の編集元

このディレクトリは、旧版PNGの設計資料として保持する。旧版PNGは`build/generate_figures.py`で再生成できる。現行本文が参照するSVGの生成元と分類は`assets/figure-sources-v1.2/`を参照する。

旧版も色だけに依存せず、すべてのboxと矢印に文字labelを付ける。出力は`assets/figures/fig01...fig19.png`。

各図の意味、参照章、事実確認先は`instructor/figure-register.md`へ記録する。生成後はA4 PDFへ組み込み、印刷寸法で文字を確認する。
