#!/usr/bin/env bash
set -u

if [[ "${1:-}" == show && "${2:-}" == -p && "${3:-}" == MainPID ]]; then
  printf '%s\n' 4242
  exit 0
fi

if [[ "${1:-}" == show && "${2:-}" == jdu-status.service && "${3:-}" == -p && "${4:-}" == InvocationID ]]; then
  printf '%s\n' m5-test-invocation
  exit 0
fi

exit 3
